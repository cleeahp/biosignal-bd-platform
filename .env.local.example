# Copy this file to .env.local and fill in your values for local testing.
# NEVER commit .env.local — it is already in .gitignore.

# Supabase connection (same as your Vercel env vars)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# LinkedIn li_at session cookie (see LINKEDIN_SETUP.md for extraction steps)
# Refresh this when LinkedIn sessions expire (every few weeks)
LINKEDIN_LI_AT=
